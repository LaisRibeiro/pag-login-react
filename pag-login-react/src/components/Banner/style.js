import {
    makeStyles
  } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
    root: {
        flexGrow: 1,
        margin: '20px',
        display: 'flex'
      },
      imageBanner:{
          width: '630px',
          height: 'flex'
      }
}));